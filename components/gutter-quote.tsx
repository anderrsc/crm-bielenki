"use client";

import { saveGutterQuote } from "@/app/(crm)/actions";
import { money } from "@/lib/utils";
import { Copy, Plus, Save, Trash2 } from "lucide-react";
import { useMemo, useState } from "react";

const products = ["Calha Beiral","Calha Platibanda","Calha Condutora","Calha Coletora","Rufo","Pingadeira","Chaminé","Condutor","Manutenção","PU MS40","Pintura"];
const thicknesses = ["0.5 mm","0.6 mm","0.7 mm","1.0 mm"];
const cuts = [150,200,250,300,350,400,450,500,600,700,800,900,1000,1200].map(x => `${x} mm`);
type Item = { product: string; thickness: string; cut: string; quantity: number; meters: number; unit_price: number };
const blank = (): Item => ({ product: "Calha Beiral", thickness: "0.5 mm", cut: "300 mm", quantity: 1, meters: 1, unit_price: 0 });

export function GutterQuote() {
  const [items, setItems] = useState<Item[]>([blank()]); const [discount, setDiscount] = useState(0); const [freight, setFreight] = useState(0); const [clientId, setClientId] = useState(""); const [notes, setNotes] = useState("");
  const subtotal = useMemo(() => items.reduce((t, i) => t + i.quantity * i.meters * i.unit_price, 0), [items]);
  const total = Math.max(0, subtotal - discount + freight);
  const change = (index: number, key: keyof Item, value: string) => setItems(current => current.map((x, i) => i === index ? { ...x, [key]: ["quantity","meters","unit_price"].includes(key) ? Number(value) : value } : x));
  const payload = JSON.stringify({ client_id: clientId, discount, freight, notes, items });
  return <div className="mx-auto max-w-[1400px]"><div className="mb-7"><p className="text-xs font-bold uppercase tracking-[.22em] text-forest">Calculadora técnica</p><h1 className="mt-2 text-3xl font-black">Orçamento de calhas</h1><p className="mt-1 text-sm text-ink/50">Valores calculados por quantidade × metragem × preço unitário.</p></div><form action={saveGutterQuote} className="grid gap-5 xl:grid-cols-[1fr_340px]"><input type="hidden" name="payload" value={payload} /><div className="space-y-4"><div className="card p-5"><label className="label">ID do cliente</label><input className="field" value={clientId} onChange={e => setClientId(e.target.value)} placeholder="Selecione ou cole o identificador do cliente" required /></div>{items.map((item, index) => <div className="card p-5" key={index}><div className="mb-4 flex items-center justify-between"><b>Item {index + 1}</b><div className="flex gap-2"><button type="button" title="Duplicar" className="button-ghost p-2" onClick={() => setItems(x => [...x.slice(0,index + 1), {...item}, ...x.slice(index + 1)])}><Copy className="h-4 w-4"/></button><button type="button" title="Excluir" disabled={items.length === 1} className="button-ghost p-2 text-red-600" onClick={() => setItems(x => x.filter((_,i) => i !== index))}><Trash2 className="h-4 w-4"/></button></div></div><div className="grid gap-4 md:grid-cols-3"><Select label="Produto" value={item.product} options={products} onChange={v => change(index,"product",v)} /><Select label="Espessura" value={item.thickness} options={thicknesses} onChange={v => change(index,"thickness",v)} /><Select label="Corte" value={item.cut} options={cuts} onChange={v => change(index,"cut",v)} /><NumberField label="Quantidade" value={item.quantity} onChange={v => change(index,"quantity",v)} /><NumberField label="Metragem" value={item.meters} onChange={v => change(index,"meters",v)} /><NumberField label="Preço / metro" value={item.unit_price} onChange={v => change(index,"unit_price",v)} /></div><div className="mt-4 text-right text-sm text-ink/50">Subtotal do item: <b className="text-ink">{money(item.quantity * item.meters * item.unit_price)}</b></div></div>)}<button type="button" className="button-ghost w-full border-dashed" onClick={() => setItems(x => [...x, blank()])}><Plus className="h-4 w-4"/> Adicionar item</button></div><aside className="card h-fit p-5 xl:sticky xl:top-24"><h2 className="text-lg font-black">Resumo</h2><div className="mt-5 space-y-4"><div className="flex justify-between text-sm"><span className="text-ink/50">Subtotal</span><b>{money(subtotal)}</b></div><NumberField label="Desconto (R$)" value={discount} onChange={v => setDiscount(Number(v))} /><NumberField label="Frete (R$)" value={freight} onChange={v => setFreight(Number(v))} /><div><label className="label">Observações</label><textarea className="field min-h-24" value={notes} onChange={e => setNotes(e.target.value)} /></div><div className="border-t pt-4"><p className="text-xs font-bold uppercase tracking-wider text-ink/45">Total</p><p className="mt-1 text-3xl font-black text-forest">{money(total)}</p></div><button disabled={!clientId || total <= 0} className="button w-full"><Save className="h-4 w-4"/> Salvar orçamento</button></div></aside></form></div>;
}
function Select({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}) { return <div><label className="label">{label}</label><select className="field" value={value} onChange={e=>onChange(e.target.value)}>{options.map(x=><option key={x}>{x}</option>)}</select></div> }
function NumberField({label,value,onChange}:{label:string;value:number;onChange:(v:string)=>void}) { return <div><label className="label">{label}</label><input className="field" type="number" min="0" step="0.01" value={value} onChange={e=>onChange(e.target.value)} /></div> }
